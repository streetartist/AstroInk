# AstroInk partition table — 16MB flash
# Name,     Type, SubType,  Offset,    Size,     Flags
nvs,        data, nvs,      0x9000,    0x6000,
phy_init,   data, phy,      0xf000,    0x1000,
factory,    app,  factory,  0x10000,   0x400000,
storage,    data, spiffs,   0x410000,  0xBF0000,
# 'storage' = LittleFS system area (mounted via esp_littlefs by label "storage").
# User/App area lives on the SD card (FATFS), configured separately.
